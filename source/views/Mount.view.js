import Preact from "preact"

import "views/Mount.view.less"

export default class Mount {
    render() {
        return (
            <div id="mount">
                <div class="Content">
                    Hello World!!
                </div>
            </div>
        )
    }
}
